class bankaccount:
    def __init__(self,name,balance):
        self.name=name
        self.balance=balance
    balance=50000 
    print("do you wanna deposit or withdraw the money")
    A=input()
    print("Ok")
    if A =="deposit money":
        print("enter the ammount")
        depositmoney=input()
        deposit=int(depositmoney)+balance
        print(deposit,"this is total balance now")
    else:
        withdrawlmoney=int(input())
        withdrawl=balance-withdrawlmoney
        try:
            if withdrawlmoney<balance:
                print("total balance now is :",withdrawl)
                print(withdrawlmoney,"rupees granted")
        except:
            if withdrawlmoney>balance:
                print("not enough balance")

